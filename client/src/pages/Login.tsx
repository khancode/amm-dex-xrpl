import * as React from 'react'

import './Login.scss'

export interface LoginProps {}

export const Login = (props: LoginProps) => {
    return (
        <main>
            <h1>Login page!</h1>
        </main>
    )
}