import * as React from 'react';

export interface ChartsProps {}

export const Charts = (props: ChartsProps) => {
    return (
        <div>
            <h1>Charts page!</h1>
        </div>
    );
 };